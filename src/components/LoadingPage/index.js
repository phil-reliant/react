import React from 'react';
import LoadingSpinner from '../LoadingSpinner';

const LoadingPage = () => (
	<div className={`loading-screen`}>
		<LoadingSpinner paddingTopLarge />
	</div>
);

export default LoadingPage;
